#include <iostream>
#include "fib3.h"

Fib Fib::fib;
/*int count[10];

void reset()
{
	for (int i = 0; i < 10; ++i)
		count[i] = 0;
}

void print()
{
	for (int i = 0; i < 10; ++i)
		std::cout << count[i] << " ";
}

int sum()
{
	int s = 0;
	for (int i = 0; i < 10; ++i)
			s += count[i];
	return s;
}*/

/*int fib(int n)
{
	std::cout << "fib(" << n << ") ...\n";
	count[n]++;
	if (n <= 1)
		return 1;
	else
		return fib(n - 1) + fib(n - 2);
}*/
